# Single source of truth for the Rust toolchain: local dev, the Windows
# build, and CI all compile with this exact version (rustup installs it
# on demand), so rustfmt and clippy verdicts never drift between machines.
[toolchain]
channel = "1.95"
components = ["rustfmt", "clippy"]
