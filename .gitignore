/target
*.local.toml

# Local session history database (dev runs create it in the working directory)
spokenrectifier-history.db

# Local business store database (dev runs create it in the working directory)
spokenrectifier-store.db

# Personal hotword dictionary (one term per line; force-add to share one)
spokenrectifier-terms.txt

# Personal scenario library (named style directives; app-owned file)
spokenrectifier-scenarios.toml

# Claude Code workflow + agent scratch (belt over .git/info/exclude suspenders)
.claude/
CLAUDE.md
docs/agents/
.scratch/

# Sandcastle runner (contains a real .env locally)
.sandcastle/

# Course-design materials contain personal identity info — keep local
docs/course_design/

# Env files never belong in git
.env
.env.*

# Node tooling
node_modules/

# Generalise the two named dev databases
*.db

# Runtime-written app artifacts (perf log and UI state land next to the exe)
spokenrectifier-perf.log
spokenrectifier-ui.toml

# Local node tooling manifests (not part of the project)
package*.json

# Release packaging output (packaging/build-release.ps1)
/dist/

