//! Shared helpers for the sr-replay, sr-rectify, and sr-eval drivers.
//! The fidelity eval itself lives in `spokenrectifier-eval` (the sr-eval
//! driver is a thin front door over its runner).

pub mod fmt;
